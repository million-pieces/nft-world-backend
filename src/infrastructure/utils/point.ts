/**
 * Class-helper.
 *
 * Using in merged-segments service for segment's dimensions calculation.
 */
export class Point {
  constructor(
    public readonly x: number,
    public readonly y: number,
  ) {}
}
